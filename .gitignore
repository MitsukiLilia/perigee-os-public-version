# macOS / editor cruft
.DS_Store
.vscode/
.idea/

# logs / deps
*.log
node_modules/

# local tooling & private deployment — must never be published
.claude/
.superpowers/
deploy.sh
sync-public.sh
cloudflare-worker.js
*.local.json

# internal dev docs (kept private)
docs/

# misc scratch
temp_scan.js
test-*
*.ps1

# local Pages redeploy helper (contains private notification config) — never publish
retry-pages-deploy.sh

# public-repo local scrub tooling (the neutralization dictionaries themselves) — never publish
tools/
